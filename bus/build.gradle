plugins {
    id 'java'
    id "com.github.dcendents.android-maven"
    id "com.jfrog.bintray"
}

group = rootProject.group
version = rootProject.busVersion

apply from: "../gradle/bintrayUpload.gradle"

//./gradlew bus:bintrayUpload